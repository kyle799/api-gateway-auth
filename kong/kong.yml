# Kong Declarative Configuration
# Use with: kong config db_import kong.yml
# Or set KONG_DECLARATIVE_CONFIG for DB-less mode

_format_version: "3.0"
_transform: true

# Example service and route configuration
# Uncomment and modify for your upstream services

# services:
#   - name: example-service
#     url: http://example-upstream:8080
#     routes:
#       - name: example-route
#         paths:
#           - /api/example
#         strip_path: true
#     plugins:
#       - name: rate-limiting
#         config:
#           minute: 100
#           policy: local
#       - name: key-auth
#         config:
#           key_names:
#             - apikey

# consumers:
#   - username: example-consumer
#     keyauth_credentials:
#       - key: your-api-key-here

# plugins:
#   # Global plugins applied to all services
#   - name: correlation-id
#     config:
#       header_name: X-Correlation-ID
#       generator: uuid
#   - name: request-transformer
#     config:
#       add:
#         headers:
#           - "X-Gateway: kong"
